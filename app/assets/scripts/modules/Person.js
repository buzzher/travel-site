					/* object oriented es6 see below 41 8:00 */

//not belong to the project travel site


/* 40 3:00class Person { //use to be function Person(fullName, favColor) { //41 8:00 constractor function
	constructor(fullName, favColor) { //js know to run the function a soon as object created 
		this.name = fullName;
		this.favoriteColor = favColor;
	}
	
	greet() {//this.greet = function() { //this reffer to either john or jane
		console.log('Hi there, my name is ' + this.name + ' and fy favorite color is ' + this.favoriteColor + '.');
	}
}

//because it's one function we making the export as a constracor function, usualy we export like export.Person
//node way - module.exports = Person; //export the function name to the require('module/Person') in app.js
export default Person; //es6 native to export
 class Person use to be like that, regular constractor function
	function Person(fullName, favColor) {
		this.name = fullName;
		this.favoriteColor = favColor;
		this.greet = function() { //this reffer to either john or jane
			console.log('Hi there, my name is ' + this.name + ' and fy favorite color is ' + this.favoriteColor + '.');
		}
	}
	*/
